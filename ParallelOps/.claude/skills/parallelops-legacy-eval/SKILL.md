---
name: parallelops-legacy-eval
description: DEPRECATED battery alias. Use parallelops-eval skill. 14-question wizard is ONLY parallelops-eval Step 1b=custom.
---

# parallelops-legacy-eval — DEPRECATED

**Do not use for `/parallelops-eval`.**

Use **`parallelops-eval`** skill instead:

- Step 1a: A1–A6 picker
- Step 1b: run selected / All / Dashboard / Custom
- 14 discovery questions **only** when user picks **Custom worktree pipeline**
